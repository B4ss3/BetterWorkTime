<Window x:Class="BetterWorkTime.App.SwitchTaskDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Switch Task"
        Width="320" Height="190"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Project" Margin="0,0,0,4"/>
        <ComboBox Grid.Row="1" x:Name="ProjectCombo"
                  DisplayMemberPath="Name"
                  Margin="0,0,0,12"
                  SelectionChanged="ProjectCombo_SelectionChanged"/>

        <TextBlock Grid.Row="2" Text="Task" Margin="0,0,0,4"/>
        <ComboBox Grid.Row="3" x:Name="TaskCombo"
                  DisplayMemberPath="Name"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Switch" Width="72" IsDefault="True"
                    Click="Switch_Click" Margin="0,0,8,0"/>
            <Button Content="Cancel" Width="72" IsCancel="True"
                    Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
